version: "3"
services:
  control:
    user: root
    image: ubuntu
    restart: always
    command: "ls /dev/ttyACM*"
    ports:
      - "11311:11311"
    volumes:
      - ./:/control_unit
    privileged: true
    devices:
      - "/dev:/dev"
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "10"